# Staging environment — would point to a staging instance in a real project
# Using same URLs here as dev since these are public demo apps
BASE_URL=https://www.saucedemo.com
API_BASE_URL=https://petstore.swagger.io/v2
UI_USERNAME=standard_user
UI_PASSWORD=secret_sauce
API_KEY=special-key
